def format_print_bench(iterations):
    i = 0
    sink = 0
    while i < iterations then
        print(i)
        sink = sink + i
        i = i + 1
    end
    return sink
end


def main(): int
    iterations = 100
    result = format_print_bench(iterations)
    print(result)
    return 0
end
