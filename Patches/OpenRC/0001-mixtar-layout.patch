diff --git a/src/librc/meson.build b/src/librc/meson.build
index e190555..0e9e748 100644
--- a/src/librc/meson.build
+++ b/src/librc/meson.build
@@ -23,7 +23,7 @@ if rc_path.length() == 0
     rc_libexecdir,
   ]
 endif
-rc_path += '/run/openrc'
+rc_path += '/System/Runtime/OpenRC'
 
 rc_h = configure_file(input : 'rc.h.in', output : 'rc.h',
   configuration : rc_h_conf_data)
diff --git a/src/librc/rc.h.in b/src/librc/rc.h.in
index f6354a6..42b559c 100644
--- a/src/librc/rc.h.in
+++ b/src/librc/rc.h.in
@@ -28,7 +28,7 @@ extern "C" {
 #define RC_LIBEXECDIR           "@RC_LIBEXECDIR@"
 #if defined(__linux__) || (defined(__FreeBSD_kernel__) && \
 		defined(__GLIBC__)) || defined(__GNU__)
-#define RC_SVCDIR               "/run/openrc"
+#define RC_SVCDIR               "/System/Runtime/OpenRC"
 #else
 #define RC_SVCDIR               RC_LIBEXECDIR "/init.d"
 #endif
diff --git a/src/openrc-init/openrc-init.c b/src/openrc-init/openrc-init.c
index b01532b..fefabf7 100644
--- a/src/openrc-init/openrc-init.c
+++ b/src/openrc-init/openrc-init.c
@@ -51,7 +51,7 @@
 #  define CLOCK_MONOTONIC CLOCK_BOOTTIME
 #endif
 
-static const char *path_default = "/sbin:/usr/sbin:/bin:/usr/bin";
+static const char *path_default = "/System/Init:/System/Commands:/System/Terminal/ZSH";
 static const char *rc_default_runlevel = "default";
 static int sigpipe[2] = { -1, -1 };
 static bool quiet = false;
@@ -182,7 +182,7 @@ static void open_shell(void)
 			if (pw)
 				shell = pw->pw_shell;
 			if (!shell)
-				shell = "/bin/sh";
+				shell = "/System/Terminal/ZSH/zsh";
 		}
 	}
 	run_program(shell);
diff --git a/src/openrc/rc.c b/src/openrc/rc.c
index 0216a6a..d989efb 100644
--- a/src/openrc/rc.c
+++ b/src/openrc/rc.c
@@ -71,7 +71,7 @@ const char *usagestring = ""
 #define INITSH                  RC_LIBEXECDIR "/sh/init.sh"
 #define INITEARLYSH             RC_LIBEXECDIR "/sh/init-early.sh"
 
-#define DEVBOOT			"/dev/.rcboot"
+#define DEVBOOT			"/System/Runtime/OpenRC/.boot"
 
 const char *applet = NULL;
 static RC_STRINGLIST *main_hotplugged_services;
diff --git a/src/shared/rc_exec.c b/src/shared/rc_exec.c
index 437d537..815229e 100644
--- a/src/shared/rc_exec.c
+++ b/src/shared/rc_exec.c
@@ -29,7 +29,7 @@ static int devnull(void)
 {
 	static int devnullfd = -1;
 	if (devnullfd < 0)
-		devnullfd = open("/dev/null", O_RDWR | O_CLOEXEC);
+		devnullfd = open("/System/Devices/null", O_RDWR | O_CLOEXEC);
 	if (devnullfd < 0) {
 		fprintf(stderr, "Failed to open /dev/null: %s", strerror(errno));
 		_exit(1);
diff --git a/sh/functions.sh.in b/sh/functions.sh.in
--- a/sh/functions.sh.in
+++ b/sh/functions.sh.in
@@ -77,7 +77,7 @@ if [ -n "$ZSH_VERSION" ]; then
 fi
 
 # Make a sane PATH
-export PATH="@LIBEXECDIR@/bin:/bin:/sbin:/usr/bin:/usr/sbin:@LOCAL_PREFIX@/bin:@LOCAL_PREFIX@/sbin$(_sanitize_path)"
+export PATH="@LIBEXECDIR@/bin:@LIBEXECDIR@/sbin:@BINDIR@:@SBINDIR@:/System/Terminal/POSIX:/System/Terminal/ZSH:@LOCAL_PREFIX@/Commands$(_sanitize_path)"
 unset _sanitize_path
 
 for arg; do
