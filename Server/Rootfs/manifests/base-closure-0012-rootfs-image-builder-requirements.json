{
  "name": "MixtarRVS Base Closure 0012",
  "status": "non-activating-rootfs-image-builder-requirements",
  "created": "2026-06-29",
  "purpose": "Install the first MixtarRVS rootfs image builder requirements contract under /System, defining image format, required inputs, exclusions, activation plan, rollback plan, and the current build blocker without creating a rootfs image.",
  "requires": [
    "/System/Base/Closure/0001-audit/manifest.json",
    "/System/Base/Closure/0002-mixtar-session-path/manifest.json",
    "/System/Base/Closure/0003-shell-runtime-and-libraries-profile/manifest.json",
    "/System/Base/Closure/0004-initramfs-loader-path-and-kernel-profile/manifest.json",
    "/System/Base/Closure/0005-initramfs-contract-and-fallback-init-shim/manifest.json",
    "/System/Base/Closure/0006-service-and-network-closure-inventory/manifest.json",
    "/System/Base/Closure/0007-mixtar-service-runner-skeleton/manifest.json",
    "/System/Base/Closure/0008-mixtar-network-profile-layer/manifest.json",
    "/System/Base/Closure/0009-mixtar-remote-access-policy/manifest.json",
    "/System/Base/Closure/0010-package-generation-backend-boundary/manifest.json",
    "/System/Base/Closure/0011-mixtar-generation-builder-dry-run/manifest.json",
    "/System/SystemTools/mixtar-generation",
    "/System/Kernel/Current/vmlinuz",
    "/System/Kernel/Current/initramfs.img",
    "/System/Tools/Current/bin",
    "/System/Libraries/MixtarRVS/Runtime/0003/lib"
  ],
  "installed_paths": [
    "/System/SystemTools/mixtar-rootfs-image",
    "/System/Config/ImageBuilder/rootfs-image.requirements"
  ],
  "commands": [
    "mixtar-rootfs-image contract",
    "mixtar-rootfs-image check",
    "mixtar-rootfs-image requirements",
    "mixtar-rootfs-image inputs",
    "mixtar-rootfs-image exclusions",
    "mixtar-rootfs-image plan",
    "mixtar-rootfs-image backend",
    "mixtar-rootfs-image readiness"
  ],
  "image_model": {
    "format": "squashfs",
    "required_builder": "mksquashfs",
    "current_builder_status": "missing",
    "future_output": "/System/Generations/<id>/rootfs.squashfs",
    "activation": "stage-only first, switch symlinks only after explicit future activation",
    "rollback": "preserve /System/Current and /System/Previous until boot-tested"
  },
  "dry_run_guarantees": [
    "Does not create rootfs.squashfs.",
    "Does not create a new generation directory.",
    "Does not mount filesystems.",
    "Does not run mksquashfs.",
    "Does not rewrite initramfs.",
    "Does not rewrite bootloader state.",
    "Does not switch /System/Current.",
    "Does not remove Alpine/OpenRC/apk fallback."
  ],
  "known_limitations": [
    "mksquashfs is missing, so squashfs generation is blocked.",
    "This stage defines requirements only; it does not build an image.",
    "The active root is still an installed mutable ext4 root.",
    "Activation, switch, boot, rollback, and garbage collection are not implemented yet.",
    "/System/Config currently resolves into compatibility /etc, so config capture still needs an explicit policy before immutable image builds."
  ],
  "next_stage": "0013-rootfs-image-builder-dependency-stage"
}
