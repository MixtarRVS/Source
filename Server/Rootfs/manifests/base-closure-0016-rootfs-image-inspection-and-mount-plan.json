{
  "name": "MixtarRVS Base Closure 0016",
  "status": "non-activating-rootfs-image-inspection-and-mount-plan",
  "created": "2026-06-29",
  "purpose": "Inspect the first MixtarRVS rootfs.squashfs image, verify required content paths and self-exclusion, and record a safe read-only mount plan without mounting or activating the image.",
  "requires": [
    "/System/Base/Closure/0015-rootfs-image-build-first-file-no-activation/manifest.json",
    "/System/SystemTools/mixtar-rootfs-image",
    "/System/Generations/0015-rootfs-image-first-file/rootfs.squashfs",
    "/System/Generations/0015-rootfs-image-first-file/manifest.json",
    "/System/Generations/0015-rootfs-image-first-file/activation.plan",
    "/usr/bin/unsquashfs"
  ],
  "installed_paths": [
    "/System/SystemTools/mixtar-rootfs-image",
    "/System/Base/Closure/0016-rootfs-image-inspection-and-mount-plan/"
  ],
  "commands": [
    "mixtar-rootfs-image inspect",
    "mixtar-rootfs-image contents-check",
    "mixtar-rootfs-image mount-plan"
  ],
  "mount_plan": {
    "mode": "inspect-only",
    "image": "/System/Generations/0015-rootfs-image-first-file/rootfs.squashfs",
    "mount_point": "/System/Runtime/Inspect/rootfs-0015",
    "command": "mount -t squashfs -o loop,ro /System/Generations/0015-rootfs-image-first-file/rootfs.squashfs /System/Runtime/Inspect/rootfs-0015",
    "activation": "none"
  },
  "required_content_paths": [
    "System",
    "System/Tools -> ../bin",
    "bin/Current",
    "bin/MixtarRVS/bin",
    "System/SystemTools -> ../sbin",
    "sbin/mixtar-rootfs-image",
    "System/Kernel/Current",
    "System/Config -> ../etc",
    "etc/Services",
    "etc/Network/current.network",
    "etc/RemoteAccess/current.remote",
    "etc/ImageBuilder/rootfs-image.requirements",
    "System/Libraries -> ../lib",
    "lib/MixtarRVS/Runtime/0003/lib",
    "bin",
    "sbin",
    "lib",
    "usr",
    "etc"
  ],
  "non_goals": [
    "Do not mount the image.",
    "Do not mount the image as root.",
    "Do not switch /System/Current.",
    "Do not rebuild initramfs.",
    "Do not change bootloader state.",
    "Do not modify the image file.",
    "Do not remove Alpine/OpenRC/apk fallback."
  ],
  "postconditions": [
    "unsquashfs -s succeeds.",
    "Required content paths are present.",
    "The image does not contain /System/Generations/0015-rootfs-image-first-file.",
    "A read-only mount command is recorded but not executed.",
    "/System/Current target is unchanged."
  ],
  "known_limitations": [
    "The image is inspected but not mounted.",
    "The image is still not booted.",
    "Switch, rollback, bootloader, and garbage collection are still missing.",
    "The generated image still contains compatibility substrate from the current mutable source root."
  ],
  "next_stage": "0017-rootfs-image-readonly-mount-inspection"
}
