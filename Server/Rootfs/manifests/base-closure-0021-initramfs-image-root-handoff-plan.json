{
  "name": "MixtarRVS Base Closure 0021",
  "id": "0021-initramfs-image-root-handoff-plan",
  "status": "planned-non-activating-initramfs-handoff",
  "purpose": "Define the initramfs handoff contract required to boot the first MixtarRVS rootfs.squashfs image while preserving the current block-root Alpine/OpenRC fallback.",
  "requires": [
    "/System/Base/Closure/0020-switch-plan-no-activation/switch-plan-no-activation-status.txt",
    "/System/SystemTools/mixtar-rootfs-image",
    "/System/Kernel/Current/vmlinuz",
    "/System/Kernel/Current/initramfs.img",
    "/System/Kernel/Current/modules",
    "/System/Generations/0015-rootfs-image-first-file/rootfs.squashfs"
  ],
  "commands": [
    "mixtar-rootfs-image initramfs-handoff-plan",
    "mixtar-rootfs-image switch-plan"
  ],
  "planned_boot_model": {
    "current": "kernel mounts block device root by UUID as ext4",
    "target": "initramfs mounts base storage, resolves /System/Current/rootfs.squashfs, mounts it read-only, adds tmpfs overlay, validates init, then switch_root",
    "fallback": "preserve current block-root generation and boot path"
  },
  "non_goals": [
    "do not rebuild initramfs",
    "do not write initramfs",
    "do not write bootloader entries",
    "do not switch /System/Current",
    "do not mount the image as /",
    "do not delete fallback generation"
  ],
  "must_report": [
    "current boot cmdline",
    "kernel profile",
    "vmlinuz readiness",
    "initramfs readiness",
    "modules directory readiness",
    "squashfs support",
    "planned mount points",
    "required initramfs features",
    "activation_allowed=false",
    "future_handoff_ready=false",
    "next required stage"
  ],
  "postconditions": [
    "initramfs handoff plan exists",
    "plan is explicitly non-activating",
    "/System/Current is unchanged",
    "rootfs inspection mountpoint is unmounted",
    "current initramfs is not modified"
  ],
  "next_stage": "0022-initramfs-handoff-prototype-no-install"
}
