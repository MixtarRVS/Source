{
  "schema": "mixtar.upstream-policy.v1",
  "policy": "openbsd-first-freebsd-fallback",
  "rules": [
    "Prefer OpenBSD source for Tier 1 core tools when mirrored and certifiable.",
    "Use FreeBSD source as fallback when OpenBSD source is missing, broader, or materially better for the tool.",
    "Never edit upstream OpenBSD or FreeBSD source in place.",
    "All adaptation belongs in Toolkit/Bridge, Manifests, Generated, Tools, or Tests.",
    "Security APIs such as pledge and unveil must be tracked as bridge semantics, not silently treated as native OpenBSD guarantees."
  ],
  "current_overrides": {
    "domainname": {
      "preferred_upstream": "openbsd",
      "fallback_upstream": "freebsd",
      "source": "Server/Userland/Toolkit/OpenBSD/src/bin/domainname/domainname.c",
      "fallback_source": "Server/Userland/Toolkit/FreeBSD/freebsd-src/bin/domainname/domainname.c",
      "certification": "certified-smoke"
    },
    "hostname": {
      "preferred_upstream": "openbsd",
      "fallback_upstream": "freebsd",
      "source": "Server/Userland/Toolkit/OpenBSD/src/bin/hostname/hostname.c",
      "fallback_source": "Server/Userland/Toolkit/FreeBSD/freebsd-src/bin/hostname/hostname.c",
      "certification": "certified-smoke"
    },
    "pwd": {
      "preferred_upstream": "openbsd",
      "fallback_upstream": "freebsd",
      "source": "Server/Userland/Toolkit/OpenBSD/src/bin/pwd/pwd.c",
      "fallback_source": "Server/Userland/Toolkit/FreeBSD/freebsd-src/bin/pwd/pwd.c",
      "certification": "certified-smoke",
      "bridge_security_apis": [
        "pledge"
      ]
    },
    "rmdir": {
      "preferred_upstream": "openbsd",
      "fallback_upstream": "freebsd",
      "source": "Server/Userland/Toolkit/OpenBSD/src/bin/rmdir/rmdir.c",
      "fallback_source": "Server/Userland/Toolkit/FreeBSD/freebsd-src/bin/rmdir/rmdir.c",
      "certification": "certified-smoke"
    },
    "sync": {
      "preferred_upstream": "openbsd",
      "fallback_upstream": "freebsd",
      "source": "Server/Userland/Toolkit/OpenBSD/src/bin/sync/sync.c",
      "fallback_source": "Server/Userland/Toolkit/FreeBSD/freebsd-src/bin/sync/sync.c",
      "certification": "certified-smoke"
    }
  },
  "bridge_security": {
    "pledge": "soft userspace policy in Bridge; records promises and blocks selected wrapped libc path operations when promises are missing",
    "unveil": "soft userspace path-prefix policy in Bridge; records allowed path prefixes and permissions for selected wrapped libc path operations",
    "limitations": [
      "Not kernel-enforced like OpenBSD.",
      "Header-only state is per translation unit until promoted to a shared Bridge runtime object.",
      "Only operations routed through Bridge wrappers are checked."
    ]
  }
}
